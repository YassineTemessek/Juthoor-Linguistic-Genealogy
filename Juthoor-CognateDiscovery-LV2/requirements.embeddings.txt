# Optional LV3 discovery dependencies (BGE-M3 + ByT5 + ANN indexing).
#
# Install (recommended in a venv):
#   python -m pip install -r requirements.embeddings.txt
#
# Notes:
# - This file is intentionally separate from `requirements.txt` so CI and basic
#   ingest/validation stay lightweight.
# - BGE-M3 will download model weights on first run (~1.7 GB).
# - ByT5-small will download model weights on first run (~300 MB).

# Core numeric stack
numpy>=2.2,<3

# ANN index (cosine / inner product)
faiss-cpu>=1.8,<2

# ByT5 form embeddings (character-level, tokenizer-free)
torch>=2.2,<3
transformers>=4.45,<5

# BGE-M3 semantic embeddings (multilingual, 100+ languages)
FlagEmbedding>=1.3
